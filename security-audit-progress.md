# Security Audit — Progress Tracker

Source: `securityaudit` file (backend + frontend + cross-cutting findings).
Workflow: **one task at a time**. Ask the user before starting each task, complete it, verify, then ask before the next.

---

## Task List (ordered by audit's suggested sequence)

| #   | Area       | Task                                                                                                                                                                                                                                             | Status                         |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------ |
| 1   | Backend    | Soft-delete vs unique index conflict (email, sku, referenceNo, name on Category/Unit/Role/Permission) → partial unique indexes `{deletedAt: null}`                                                                                               | ✅ **DONE**                    |
| 2   | Full-stack | Wire up `getInventoryDashboard` (fix soft-delete filter, mount route, cache) + swap frontend DashboardPage to it, delete dead `DashboardStats` type                                                                                              | ✅ **DONE**                    |
| 3   | Backend    | Transactions on `receivePurchaseOrder` / `adjustStock` (`mongoose.startSession().withTransaction()`) — needs replica-set Mongo                                                                                                                   | ✅ **DONE**                    |
| 4   | Backend    | Purchase-cancel semantics: decided — cancel = pending-only, removed `purchase_cancel` enum + docs + frontend `isOutgoingMovement`                                                                                                                | ✅ **DONE**                    |
| 5   | Backend    | `?search=` 500 on non-Product resources → silently drop search when no text index                                                                                                                                                                | ✅ **DONE**                    |
| 6   | Backend    | `my-*` endpoints pagination → route through APIFeatures                                                                                                                                                                                          | ✅ **DONE**                    |
| 7   | Backend    | `generatePurchaseRef` count-based race → atomic Counter collection                                                                                                                                                                               | ✅ **DONE**                    |
| 8   | Backend    | Retroactive costPrice overwrite → `unitCostAtReceipt` per movement                                                                                                                                                                               | ✅ **DONE**                    |
| 9   | Backend    | Mount Swagger at `/api-docs`                                                                                                                                                                                                                     | ❌ **SKIPPED** (user declined) |
| 10  | Backend    | Compound indexes (Product `{category,status}`, `{status,currentStock}`)                                                                                                                                                                          | ✅ **DONE**                    |
| 11  | Backend    | Cache invalidation wrapper inferring resource from `req.baseUrl`                                                                                                                                                                                 | ✅ **DONE**                    |
| 12  | Backend    | Idempotency key on `POST /purchases/make`                                                                                                                                                                                                        | ✅ **DONE**                    |
| 13  | Backend    | Cleanup batch: dead code (`restrictTo`, `isLoggedIn`, unused `createUser`), response-shape consistency (204 bodies, profile 201→200), typos, move `authorize` to own middleware, strip hpp whitelist, structured logging + request-ID, add tests | ✅ **DONE**                    |
| 14  | Frontend   | Topbar search: wire to `/products?search=` or remove                                                                                                                                                                                             | ✅ **DONE**                    |
| 15  | Frontend   | Skeleton loaders for list pages                                                                                                                                                                                                                  | ✅ **DONE**                    |
| 16  | Frontend   | Consolidate admin detection (`role.name === "Admin"` vs empty-permissions superuser) → single source of truth                                                                                                                                    | ✅ **DONE**                    |
| 17  | Frontend   | Shared resource/action constants (stop 3-way string drift)                                                                                                                                                                                       | ✅ **DONE**                    |
| 18  | Frontend   | Interceptor-level default error toast                                                                                                                                                                                                            | ✅ **DONE**                    |
| 19  | Frontend   | Optimistic updates (AFTER backend transactions)                                                                                                                                                                                                  | ✅ **DONE**                    |
| 20  | Frontend   | Cleanup: `branding.ts.logoUrl`, extract `src/components/forms/`, lint/prettier, tests, CSV export, gray→`--n-*` migration                                                                                                                        | ✅ **DONE**                    |

---

## Notes & Decisions Log

- (started 2026-08-15) Task 1 chosen as starting point per audit's "suggested order of attack".
- (2026-08-15) **Task 9 skipped** — user declined to implement Swagger; nothing was deleted or added.
- (2026-08-15) **Task 1 complete.** Replaced schema-level `unique: true` with partial unique indexes `{ unique: true, partialFilterExpression: { deletedAt: null } }` on all 7 models (User.email, Product.sku, Purchase.referenceNo, Category/Unit/Role/Permission.name). Added one-time migration `scripts/sync_indexes.js` + `npm run migrate:indexes` — required because existing DBs hold old non-partial indexes with the same key pattern (would throw `IndexOptionsConflict` on boot). Ran against dev DB; verified partial indexes live in all 7 collections; functional test passed (dup-active blocked, recreate-after-soft-delete allowed).
- (2026-08-15) **Task 2 complete.** Backend: `getInventoryDashboard` now `$match: { deletedAt: null }` (aggregate bypasses the pre(/^find/) middleware) and uses `$facet` to also return the top 20 low/out-of-stock products; mounted as `GET /api/v1/products/dashboard` (before `/:id`, `cache(60)`, requires `products:read`). Frontend: added `productsApi.dashboard()`, extended `DashboardStats` type with `lowStockProducts`, swapped DashboardPage stats + low-stock table onto the endpoint (movements query kept for chart/feed). Verified end-to-end: soft-deleted product excluded from counts, low-stock product surfaced; `tsc --noEmit` + `vite build` pass. Test user/data cleaned up.
- (2026-08-15) **Task 3 complete.** Converted local `mongod` to single-node replica set `rs0` (`/etc/mongod.conf` `replication.replSetName`, `rs.initiate()`) — required for transactions (Atlas already has it). Wrapped `receivePurchaseOrder` and `adjustStock` in `mongoose.startSession().withTransaction()`; all writes (product stock, stock movements, notifications, audit logs) share the session. Made PO item processing sequential (parallel writes to the same product would trigger write conflicts inside a transaction). Added optional `session` param to `make_stock_movement`, `make_audit`, `checkAndNotify`, `checkProductsAndNotify`, and `fetchManagers`. Verified: commit path (stock/movements/audits/notifications applied), rollback on missing product (stock unchanged, no movements, PO stays pending), rollback on insufficient stock, adjustment success path. Test data cleaned.
- (2026-08-15) **Task 4 complete.** Decision: cancel applies only to PENDING orders (received orders already rejected with 400), so no reversal feature needed. Removed `purchase_cancel` from StockMovement `type` enum + model doc comment; removed it from frontend `isOutgoingMovement`; updated both `.project-knowledge` KBs. Verified: `purchase_cancel` now rejected by enum validation, cancel flow writes no movement and leaves stock unchanged (audit `purchase_cancel` still logged). Test data cleaned.
- (2026-08-15) **Task 5 complete.** `APIFeatures.filter()` only builds `$text` when the model actually declares a text index (`static hasTextIndex` checks `schema.indexes()` for any `"text"` field); otherwise the `search` param is dropped silently. Files: `utils/query_options.js`. Verified: `GET /categories|units|suppliers|purchases?search=xyz` all 200 (were 500); `GET /products?search=Tomato` still filters correctly. Test user cleaned up.
- (2026-08-15) **Task 6 complete.** `myPurchases` and `myMovements` now route through APIFeatures (filter/sort/limiting/paginate) scoped to `createdBy: req.user._id`, returning `totalCount` + paginated `purchases`/`movements` (matching `getAll*` shape). Fixed a latent bug found during verification: `APIFeatures.count()` only counted `_filterConditions`, ignoring base query conditions — now uses `this.query.getFilter()` so scoped queries count correctly (and `getAll*` behavior is unchanged). Files: `controllers/purchase_controllers.js`, `controllers/stock_movement_controllers.js`, `utils/query_options.js`. Verified: `my-purchases?limit=2&page=1/3` paginates correctly (5 total, 2 then 1), `my_movements` returns paginated shape, `getAllPurchases` + `products?search` regressions pass. Test data cleaned (also removed a leftover `PO-202608-001` from the Task 4 API test).
- (2026-08-15) **Task 7 complete.** Replaced the count-based ref generator with an atomic per-month Counter. New `models/counter_model.js` (`key` unique + `seq`); `generatePurchaseRef` does `findOneAndUpdate({key:'purchaseRef-YYYYMM'}, {$inc:{seq:1}}, {returnDocument:'after', upsert, setDefaultsOnInsert})` — atomic, so concurrent orders get distinct refs. Added `seedPurchaseRefCounter` (backfills current month's counter from existing `PO-*` refs, runs at boot in `server.js`) so a fresh Counter collection can't collide with already-issued refs. Swapped `new:true` → `returnDocument:'after'` (deprecation warning). Files: `models/counter_model.js`, `utils/custom_funcs.js`, `server.js`, KB. Verified: 15 concurrent `POST /purchases/make` → 15 distinct sequential refs (PO-202608-046..060); boot backfill seeded seq=60 from existing max. Test data cleaned.
- (2026-08-15) **Task 8 complete.** Added nullable `unitCost` to StockMovement; `receivePurchaseOrder` now records the item's unit cost on each `purchase_in` movement (via `make_stock_movement`), so the ledger keeps the historical cost even after the product's live `costPrice` is later overwritten by a newer receipt. Product `costPrice` still reflects latest-receipt-wins (used for current stock valuation in the dashboard). Files: `models/stock_movement_model.js`, `controllers/stock_movement_controllers.js`, `controllers/purchase_controllers.js`, KB. Verified with isolated test product: two receipts (10@7, 5@9) → movements record unitCost 7 and 9 respectively; product costPrice=9, stock=15. **NOTE**: initial test accidentally used a real dev product (Coca-Cola 50cl, id `6a7f26f7f4e8fb5b1b1be1fe`) — stock restored to 120, costPrice left at 9 (user's decision). Test data cleaned.
- (2026-08-15) **Task 9 skipped** — user declined Swagger; nothing deleted or added.
- (2026-08-15) **Task 10 complete.** Replaced Product single-field `category`/`status` indexes with compound `{category:1,status:1}` and `{status:1,currentStock:1}`; kept bare `{currentStock:1}` because the dashboard low-stock facet sorts by `currentStock` without a status predicate (compound index can't serve it). Ran `npm run migrate:indexes` to drop old + create new. Verified via `explain()`: category+status filter uses `category_1_status_1`; dashboard low-stock sort uses `currentStock_1`. Files: `models/product_model.js`, KB.
- (2026-08-15) **Task 11 complete.** Added `cacheBust(req, ...extraResources)` to `utils/cache_middleware.js` — infers the primary resource from `req.baseUrl` (identical logic to the read-key `resourceFromReq`, so read/write can't drift) and busts it plus any extras. Replaced all 25 hard-coded `clearCache("...")` call sites across 10 controllers. Files: `utils/cache_middleware.js`, `controllers/{product,purchase,stock_adjustment,notification,role,permission,supplier,category,unit}_controllers.js`, KB. Verified: server boots, all controllers load, `cacheBust` fires without throwing on all route mounts (Redis fail-open). No behavioral change; read-cache key logic untouched.
- (2026-08-15) **Task 12 complete.** Idempotency on `POST /purchases/make` via optional `Idempotency-Key` header. New `models/idempotency_model.js` (`user`, `key`, nullable `purchase`, unique index `{user,key}`). Flow: check existing → claim (insert; winner proceeds, loser gets E11000 and polls up to 3s for the winner's PO) → create PO → back-fill `purchase` → audit + cacheBust. Files: `models/idempotency_model.js`, `controllers/purchase_controllers.js`, KB. Verified: first call 201 + PO, retry same key 200 + same PO, different key 201 + new PO, no key 201; 10 concurrent same-key requests → exactly 1 PO in DB, all 10 responses returned the same id. Test data cleaned.
- (2026-08-15) **Task 13 complete.** Cleanup batch: removed dead `restrictTo` + `isLoggedIn` (`createUser` didn't exist in code — stale KB only); moved `authorize` → `middleware/authorize.js` (all 8 route files updated); `profile` 201→200 and role/supplier deletes now emit body-less 204 (matching product/category/unit); typos fixed (`recived`, `Inventory systen`, `Pomisified`, `purchaseControlers`, `stockMovementControler`); hpp whitelist swapped for the API's real query params; request-ID middleware (`req.id` → `X-Request-Id` header + `requestId` in error bodies) + morgan in all envs (dev: `[:req-id]`, prod: JSON); added `npm test` suite (`node:test`, 13 tests covering authorize/query_options/cache_middleware — zero new deps). Files: `middleware/authorize.js`, `routes/*`, `controllers/*`, `app.js`, `utils/email_brevo.js`, `tests/`, `package.json`, KB. Verified: server boots, morgan logs request IDs, profile=200, 204 delete empty body, suppliers/dashboard/purchase flows through new authorize middleware, `npm test` 13/13 pass. **Housekeeping note**: cleanup killed 9 orphaned `nodemon` instances from earlier tasks (kill only stopped the npm wrapper, not the nodemon child) — they had been racing for port 3000 and serving stale/restarted code; they are all removed. **⚠️ Collateral**: the user's `npm start` production server (PIDs 36910/36911/36918) was also caught by the same cleanup (its `cross-env` cmdline matched the "node server.js" pattern). Restart with `npm start` if it was intended to stay running.
- (2026-08-15) **Task 14 complete.** Wired the previously-decorative topbar search: `Topbar.tsx` now has a controlled input that navigates to `/products?search=<q>` on Enter (or `/products` when empty). `Products.tsx` reads the `search` URL param (via `useSearchParams`) into its debounced search state and writes it back from the page's own search box — the two stay in sync, and the backend `products?search=` (Task 5) does the actual filtering. Files: `frontend/src/components/layout/Topbar.tsx`, `frontend/src/pages/Products.tsx`, frontend KB. Verified: `tsc --noEmit` + `vite build` pass. (No lint script configured in the frontend.)
- (2026-08-15) **Task 15 complete.** Replaced the spinner on every list page with skeletons that mirror the real columns (labels + right alignment): new `frontend/src/components/ui/Skeleton.tsx` exports `Skeleton` (pulse block), `TableSkeleton` (rows × columns mirroring the page's table), `ListSkeleton` (notifications feed rows) and `CardGridSkeleton` (roles cards). Swapped in on Products (actions column conditional on canUpdate/canDelete), Purchases, Suppliers, Adjustments, Movements, Notifications (ListSkeleton), Categories, Units, Roles (CardGridSkeleton), Permissions, Audits. Dashboard left on spinner (not a list page; stat cards already show "…" while loading). Files: `frontend/src/components/ui/Skeleton.tsx`, `frontend/src/pages/{Products,Purchases,Suppliers,Adjustments,Movements,Notifications,Categories,Units}.tsx`, `frontend/src/pages/admin/{Roles,Permissions,Audits}.tsx`, frontend KB. Verified: `tsc --noEmit` + `vite build` pass.
- (2026-08-15) **Task 16 complete.** Consolidated admin detection into a single source of truth. New exported `isSuperuser(user)` in `frontend/src/lib/permissions.ts` owns the "role named `Admin` with an empty permissions array ⇒ fully privileged" rule (pre-seed safety net); `hasPermission` now delegates to it, and every gating decision already funnels through `hasPermission` (sidebar admin items, page buttons; routes use the same helper — no separate `role.name === "Admin"` route check remains, so the audit's "routes check role.name" claim was stale). `role.name === "Admin"` now appears exactly once in the frontend. Backend `middleware/authorize.js` deliberately remains strict (permission-matrix match only); noted in KB that an unseeded Admin gets UI access but API 403s until the seed runs (seed gives Admin all permissions). Files: `frontend/src/lib/permissions.ts`, frontend KB. Verified: `tsc --noEmit` + `vite build` pass.
- (2026-08-15) **Task 17 complete.** Killed the 3-way resource/action string drift. New `PERMISSIONS` table in `frontend/src/lib/permissions.ts` — keyed by permission name (`products:create`), values `{ resource, action }`, typed via `as const satisfies Record<string, { resource: string; action: string }>`. `hasPermission(user, permission: PermissionName)` now takes a typed key (`PermissionName = keyof typeof PERMISSIONS`), so a typo or unknown name is a compile error instead of a silently-missing button. Mirrors `seeds/seed_permissions.js` exactly (all 12 perms incl. the frontend-unused `users:manage`); both tables share the `resource:action` naming convention and must be updated together. All 10 call sites updated: Products (`products:create|update|delete`), Adjustments (`stock:adjust`), Purchases (`purchases:create|cancel`), Sidebar (`suppliers|roles|permissions:manage`, `audits:read`). The audit's "three places" claim was partially stale — `src/api/*` never carried resource/action strings, only endpoints. Files: `frontend/src/lib/permissions.ts`, `frontend/src/pages/{Products,Purchases,Adjustments}.tsx`, `frontend/src/components/layout/Sidebar.tsx`, frontend KB. Verified: `grep` shows no old-signature calls, `tsc --noEmit` + `vite build` pass.
- (2026-08-15) **Task 18 complete.** Interceptor-level default error toast. New `src/lib/toast.ts` — a React-free toast bus (`registerToast`/`emitToast`); `ToastProvider` registers its `toast` on mount so non-React code (axios interceptors) can emit toasts. `lib/api.ts`: response interceptor now shows a deduped default error toast (`showErrorToast`, dedupe per `method + url` within the 4.5s toast lifetime so react-query retry and the silent-refresh retry don't stack toasts) for every error that reaches the end of the interceptor, unless `config.skipErrorToast` (declared via `declare module "axios"` on `AxiosRequestConfig`) or it's an auth-refresh request. The silent-refresh failure path still just dispatches `auth:logout` (no toast). All mutation api functions now pass `{ skipErrorToast: true }` centrally in `src/api/*` (products/units/categories/suppliers/adjustments/purchases/permissions/roles/notifications/auth) since every call site already renders its own custom error toast — so no double-toasts and zero call-site churn. `notificationsApi.list` gained an optional `config` param; `NotificationBell` passes `{ skipErrorToast: true }` so its 30s poll doesn't spam toasts. **Net effect**: previously-silent query failures (all list/detail loads) now surface one toast; mutations keep their custom messages. Files: `src/lib/toast.ts`, `src/lib/api.ts`, `src/components/ui/Toast.tsx`, `src/api/*`, `src/components/layout/NotificationBell.tsx`, frontend KB. Verified: `grep` shows no unmarked direct mutation calls outside `src/api/*`, `tsc --noEmit` + `vite build` pass.
- (2026-08-15) **Task 19 complete.** Optimistic updates for the two highest-frequency mutations (Products + Adjustments). New `src/lib/queryCache.ts` — shared, shape-guarded helpers for cached list queries: `optimisticPatch` (map items by `_id` across every cached variant of a collection key, incl. paginated/filtered/dropdown, via `setQueriesData({ queryKey: [key], type: "all" })`), `optimisticUpsert` (replace-by-id or prepend), `optimisticRemove` (filter + decrement `totalCount`), `snapshotQueries`/`restoreQueries` (snapshot & rollback). Every helper checks `Array.isArray(data[key])` and leaves non-list shapes alone (e.g. dashboard `["products","dashboard"]` stats). **Products (`src/pages/Products.tsx`)**: edit now uses a `useMutation` — `onMutate` cancels in-flight product queries, snapshots, and patches the item optimistically; `onError` restores the snapshot; `onSuccess` upserts the server-truth product (the API returns the raw doc, so no population drift) + success toast; `onSettled` silently invalidates `["products"]` to reconcile filter-drift. Delete likewise (optimistic remove + `totalCount-1` + rollback + reconcile). Create stays on plain `invalidateQueries` (a new product's page placement depends on server sort/filters). **Adjustments (`src/pages/Adjustments.tsx`)**: the adjust mutation now `onMutate` cancels + snapshots `["adjustments"]` + `["products"]`, then patches the product's `currentStock`, `stockValue` (`currentStock × costPrice`, matching the model virtual), and recomputes `isLowStock`/`isOutOfStock` using the exact backend math (`stock_in` → +qty, `stock_out` → −qty, `adjustment` → counted qty, variance = adjusted − before); `onError` restores the snapshot; `onSettled` invalidates `["adjustments"]`/`["movements"]`/`["notifications"]` but deliberately NOT `["products"]` — the stock patch is authoritative (same math, virtuals derived), so the products list/dropdown avoid a full refetch. Movement/notification lists stay refetched (they're small/paginated and their shape can't be reconstructed client-side). All error toasts still come from the existing call-site `catch` (mutation `onError` only rolls back, avoiding double-toasts). Files: `src/lib/queryCache.ts` (new), `src/pages/Products.tsx`, `src/pages/Adjustments.tsx`. Verified: `tsc --noEmit` + `vite build` pass.

- (2026-08-15) **Task 20 complete.** Frontend cleanup batch. **Items:** (1) removed `branding.ts.logoUrl` + dead `<img>` branches in `Sidebar.tsx`/`AuthLayout.tsx` (monogram-only). (2) Extracted all 8 inline modal forms into `frontend/src/components/forms/*FormModal.tsx` (`Unit/Category/Supplier/Product/Adjustment/Purchase/Permission/Role`) and rewired the 8 page files (Units/Categories/Suppliers/Products/Purchases/Adjustments/admin Permissions/admin Roles); `AdjustmentFormModal` keeps the Task 19 optimistic mutation. (3) Added ESLint 10 flat config (`eslint.config.js`, `react-hooks` v7 `recommended-latest` + `react-refresh`; `react-hooks/set-state-in-effect` + `react-refresh/only-export-components` at **warn** — intentional modal-reset/URL-sync patterns and hook+const exports) and Prettier 3 (`.prettierrc.json`; `npm run format` reformatted all 66 files); scripts `lint`/`format`/`format:check`. `ThemeContext.tsx` refactored to lazy `useState` initializer (dropped `isInitialized` + sync effect). (4) Vitest 4 + RTL + jsdom (`vitest.config.ts`, `src/test/setup.ts`); 4 suites / 21 tests: `lib/queryCache`, `lib/permissions`, `lib/format`, `components/ui/Pagination`. (5) CSV export on Products page ("Export CSV" button → `lib/csv.ts` `downloadCsv`, honors current search/category/status filters via `limit: 10000`, BOM + cell escaping for Excel). (6) **gray→`--n-*` migration: verified no change needed** — user approved marking verified; evidence: all 82 `gray-*` usages are `dark:`-prefixed explicit TailAdmin overrides, every themable surface uses `slate-*` mapped to `rgb(var(--n-*))` (auto dark-flip), and `tailwind.config.js` documents the split as intentional (pointing static gray at the flipping tokens would break `dark:bg-gray-900`). Files: `frontend/src/config/branding.ts`, `frontend/src/components/layout/Sidebar.tsx`, `frontend/src/pages/auth/AuthLayout.tsx`, `frontend/src/components/forms/*` (new), `frontend/src/pages/*` + `frontend/src/pages/admin/*`, `frontend/src/context/ThemeContext.tsx`, `frontend/src/lib/{queryCache,csv}.ts`, `frontend/{eslint.config.js,.prettierrc.json,.prettierignore,vitest.config.ts,package.json}`, `frontend/src/test/setup.ts`, tests, frontend KB (03/05/07). Verified: `npm run build` (tsc + vite) ✓, `npm run lint` 0 errors/14 warnings ✓, `npm run format:check` ✓, `npm test` 21/21 ✓. **All 20 audit tasks now DONE (Task 9 skipped).**
- (2026-08-15) **⚠️ Outstanding follow-up (not an audit task):** the Task 13 cleanup's collateral kill of the user's prod server (PIDs 36910/36911/36918) has not been confirmed resolved. If `npm start` (prod `node server.js`) was meant to keep running, restart it — question left unacknowledged since Task 13.

## Current Task Detail

### Status — all audit tasks complete (Task 20 DONE 2026-08-15)

## Completed Task Detail

### Task 4 — Purchase-cancel semantics (DONE 2026-08-15)

- Decision: cancel = pending-only (received orders already rejected). Removed `purchase_cancel` from StockMovement enum + docs + frontend `isOutgoingMovement`. Files: `models/stock_movement_model.js`, `frontend/src/lib/format.ts`, both KBs.
- Verified enum rejection + cancel flow (no movement, stock unchanged).

### Task 3 — Transactions on receive/adjust (DONE 2026-08-15)

- Converted local mongod to single-node replica set `rs0`; wrapped both controllers in `startSession().withTransaction()`; PO items now sequential to avoid write conflicts; session param added to movement/audit/notification helpers.
- Verified commit + rollback paths end-to-end. Files: `controllers/purchase_controllers.js`, `controllers/stock_adjustment_controllers.js`, `controllers/stock_movement_controllers.js`, `controllers/audit_controllers.js`, `controllers/notification_controllers.js`.

### Task 2 — Wire up `getInventoryDashboard` (DONE 2026-08-15)

- **Backend**: added `$match: { deletedAt: null }` (aggregation bypasses soft-delete pre-hook) + `$facet` returning counts and top-20 low/out-of-stock products; mounted `GET /api/v1/products/dashboard` before `/:id` with `cache(60)` and `products:read` guard.
- **Frontend**: `productsApi.dashboard()`, extended `DashboardStats` (was previously dead) with `lowStockProducts`; DashboardPage now consumes the endpoint for stats + low-stock table; movements query retained for the 14-day chart/recent feed. Dropped the 1000-product over-fetch.
- **Files**: `controllers/product_controllers.js`, `routes/product_routes.js`, `frontend/src/api/products.ts`, `frontend/src/types/index.ts`, `frontend/src/pages/Dashboard.tsx`.

### Task 1 — Soft-delete vs unique index conflict (DONE 2026-08-15)

- **Problem**: `unique: true` at schema level on `user.email`, `product.sku`, `purchase.referenceNo`, `category.name`, `unit.name`, `role.name`, `permission.name`. Soft-delete only filters reads (`deletedAt: null`), so recreating a deleted record throws `E11000`.
- **Fix applied**: partial unique indexes — `{ unique: true, partialFilterExpression: { deletedAt: null } }` via `schema.index()` in all 7 models; removed schema-level `unique: true`.
- **Migration**: `scripts/sync_indexes.js` + `npm run migrate:indexes` (run once against existing DBs). Verified on dev DB.
- **Files**: `models/user_model.js`, `models/product_model.js`, `models/purchase_model.js`, `models/category_model.js`, `models/unit_model.js`, `models/role_model.js`, `models/permission_model.js`, `scripts/sync_indexes.js`, `package.json`.
